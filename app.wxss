@import "./styles/iconfont.wxss";

/* 在微信小程序中不支持通配符* */
page,view,text,swiper,swiper-item,image,navigator {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 主题颜色 */
page {
  /* 定义主题颜色 */
  --themeColor: #eb4450;
  /* 定义统一字体大小 */
  /* 假设设计稿375px中字体大小14px，则应该使用28rpx */
  font-size: 28rpx;
}